
const area = function(length, width) {
    return length * width 
}

const area2 = (length, width) => { length * width }

const arr = [1,2,3]

arr.forEach((item, i) => { console.log(item) })

arr.map(item => {
    return item * 2
})